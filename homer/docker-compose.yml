version: "3.7"

services:
  homer:
    image: b4bz/homer
    container_name: homer
    restart: unless-stopped
    volumes:
      - ./data:/www/assets:rw
    ports:
      - 9001:8080
    networks: 
      - dashboard

networks: 
  dashboard:
    